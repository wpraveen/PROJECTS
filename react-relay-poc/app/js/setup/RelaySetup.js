//import Relay from "react-relay";

// Relay.injectNetworkLayer(new Relay.DefaultNetworkLayer("http://localhost:8888", {
//     fetchTimeout: 30000, // Timeout after 30s.
//     retryDelays: [5000] // Only retry once after a 5s delay.
// }));
